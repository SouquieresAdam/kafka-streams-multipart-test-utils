# Default Kafka Streams version. Override with -PkafkaVersion=<x.y.z>.
# See README "Compatibility matrix" for which versions ship pre-built variants.
#
# 4.4.0-SNAPSHOT is the trunk version that the multi-partition fork was built
# against (see SouquieresAdam/kafka multi-partition-test-driver-v2). It must be
# present in mavenLocal() — run `./gradlew publishToMavenLocal -x signMavenPublication`
# in the Kafka clone before building this project.
kafkaVersion=4.4.0-SNAPSHOT

# Test dependency versions
junitVersion=5.13.4
hamcrestVersion=3.0
mockitoVersion=5.20.0
slf4jVersion=2.0.17
