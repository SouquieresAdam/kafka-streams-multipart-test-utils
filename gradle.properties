# Default Kafka Streams version. Override with -PkafkaVersion=<x.y.z>.
# See README "Compatibility matrix" for which versions ship pre-built variants.
#
# Kafka 4.2.0 backport branch — the most recent published release at the time
# of writing. Resolved from Maven Central; no mavenLocal() prereq.
kafkaVersion=4.2.0

# Test dependency versions
junitVersion=5.13.4
hamcrestVersion=3.0
mockitoVersion=5.20.0
slf4jVersion=2.0.17
